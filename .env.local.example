# Supabase 專案設定
# 於 Supabase Dashboard > Project Settings > API 取得
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 注意：
# - 目前所有資料存取都在伺服器端（Server Components / Server Actions）進行，
#   一律使用 Service Role Key，尚未使用瀏覽器端的 anon key，因此不需要設定
#   NEXT_PUBLIC_SUPABASE_ANON_KEY。
# - SUPABASE_SERVICE_ROLE_KEY 擁有完整資料庫權限，絕對不能加上 NEXT_PUBLIC_
#   前綴、不能出現在任何會送到瀏覽器的程式碼裡，只能設定在伺服器端環境變數
#   （本機開發放在 .env.local，Vercel 則設定在 Project Settings > Environment Variables）。
